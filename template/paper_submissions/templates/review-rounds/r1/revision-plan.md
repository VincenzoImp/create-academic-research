# Revision Plan

Use `concern-map.csv`, `linked-work.csv`, and `manuscript-change-map.csv` to
plan revisions. New experiments, analyses, artifacts, or citations are executed
in their canonical workflow folders before response text cites them.
