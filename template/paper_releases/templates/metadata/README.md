# Release Metadata

Store release metadata here: release title, authors, license, citation,
repository destination, DOI or archival identifier, artifact scope,
anonymization mode, included paths, excluded paths, and support contact.

Metadata must be generated from `release.yaml`, `source-map.csv`, and canonical
project records. Do not edit release staging as a second source of truth.
